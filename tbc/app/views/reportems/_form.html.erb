<%= form_for(@reportem) do |f| %>
  <% if @reportem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reportem.errors.count, "error") %> prohibited this reportem from being saved:</h2>

      <ul>
      <% @reportem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fecha %><br>
    <%= f.date_select :fecha %>
  </div>
  <div class="field">
    <%= f.label :trayectos %><br>
    <%= f.number_field :trayectos %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
